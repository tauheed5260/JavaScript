let index = 0
while (index <= 10) {
    // console.log(`value of index is ${index}`);
    index = index + 1
}
let myArr = ["Tauheed", "Faiz", "Umar"]
let arr = 0
while (arr < myArr.length) {
    // console.log(`value is ${myArr[arr]}`);
    arr = arr + 1
}

let score = 11
do {
    console.log(`Score is ${score}`);
    score ++
} while (score <= 19);